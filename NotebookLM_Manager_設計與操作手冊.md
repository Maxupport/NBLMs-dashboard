# NBLMs LinkStation 系統設計與操作手冊

本文件旨在記錄「NBLMs LinkStation | NBLM 連結總站」的技術架構與操作指南，方便未來維護、交接與佈署。

## 1. 系統架構與技術棧

本系統採用全端統一架構，無縫整合前後端邏輯，並針對 Serverless 佈署進行了最佳化：

- **核心框架**：Next.js 14+ (App Router, TypeScript)
- **前端樣式**：Tailwind CSS v4 (使用玻璃透視 Glassmorphism 設計語彙)
- **資料庫**：**Turso (Cloud SQLite)** 使用 `@libsql/client`。
  - 本地開發時，預設寫入根目錄的 `local.db` 檔案。
  - 佈署至 Vercel 等 Serverless 平台時，只需配置環境變數，系統將自動無縫對接雲端節點，防止資料隨進程關閉而遺失。
- **身分驗證**：JSON Web Tokens (JWT, `jose` 函式庫) 搭配 `bcryptjs` 進行密碼雜湊。狀態管理透過 HTTP-Only Cookie 實作，確保高安全性。

> [!TIP]
> **為什麼選用 Turso 而非原生的 SQLite 檔案？**
> 因為 Vercel 這類的 Serverless 平台在每次回覆請求完畢後都會關閉進程，且硬碟為唯讀/暫存性質。若使用純 `.db` 檔案，所有新建的帳號與專案會在幾分鐘後被徹底清除。使用 Turso 完美保留了 SQLite 的輕量特性，同時解決了無狀態（Stateless）環境的資料留存問題。

---

## 2. 資料庫綱要 (Schema)

系統包含五個核心資料表：

1. `users`：儲存正式會員（含系統管理員 Admin 與一般成員 Member）。
2. `registration_applications`：儲存來自外部透過註冊頁面送出的申請單（Pending/Approved/Rejected）。
3. `projects`：專案資料夾，由 Admin 建立。
4. `project_members`：中介型資料表（Many-to-Many），紀錄哪些 User (Member) 被授權檢視哪些 Project。
5. `notebook_links`：儲存專案底下的 NotebookLM 外部連結實體。

---

## 3. 權限角色說明 (Role-based Access Control)

系統嚴格區分兩種權限層級：

### 👑 Admin (管理員)
- 系統初始化時，預設會建立一組管理員帳號：`admin` / 密碼：`Admin@123456`。
- **全域存取**：能看見所有專案與所有 Notebook 連結。
- **管理權限**：
  - 核准或拒絕外部的帳號申請。
  - 主動建立系統帳號（系統會自動幫該使用者產生 12 碼高強度密碼）。
  - 新增/修改/刪除「專案 (Project)」。
  - 指派「專案權限」，決定哪些專案開放讓哪些 Member 觀看。
  - 新增/刪除「NotebookLM 連結」。

### 👤 Member (一般夥伴成員)
- 登入後，可看見**自己建立**的專案以及**授權**給他的專案（由管理員建立的專案會隱藏真實身分，統一顯示為「Max (管理員)」）。
- **去中心化協作**：每個 Member 皆可建立屬於自己的專案，並自由管理專案內的 NotebookLM 連結。
- **針對被授權的專案**：若為別人建立並授權給您的專案，則您僅具備唯讀權限，無法刪除別人的連結或修改專案設定。

---

## 4. 操作指南表

### A. 管理員首次登入與密碼更改
1. 使用預設帳號 `admin` 與預設密碼 `Admin@123456` 登入。
2. （*建議未來由資料庫或擴充更新密碼功能*）目前若需更改管理員密碼，需直接在伺服器環境操作。

### B. 處理成員帳號
系統支援「雙向」發放帳號：
- **合作夥伴自行申請**：
  - 請夥伴從首頁切換至「申請帳號」填寫信箱與備註。
  - 管理員登入 Dashboard 後，點擊右上角「帳號管理中心」->「待審核申請」進行**核准**。
- **管理員主動建立**：
  - 於「帳號管理中心」左下方填寫使用者名稱。
  - 點擊「建立帳號」，系統會顯示一組 **12 碼自動生成的強安全密碼**。
  - 游標移至密碼區塊，點擊「📋 複製」，並將帳密私下傳送給合作夥伴。

### C. 建立專案與開放權限
1. 在 Dashboard 首頁總覽，點擊「馬上建立新專案」。
2. 建立新專案後，您可以從左方 Sidebar 或首頁卡片進入該專案。
3. 進入專案頁面後，點擊右上角「👥 專案權限」。
4. 勾選欲開放觀看的成員名單，點擊「儲存」。**未被勾選的成員，完全無法得知此專案的存在。**

### D. 收錄 NotebookLM 連結
1. 在 NotebookLM 系統中，對目標筆記本點選「分享」並複製連結。
2. 回到 Dashboard 並進入您想歸類的專案，點擊「+ 新增 Notebook 連結」。
3. 貼上網址並輸入適當的標題即可完成收錄。

---

## 5. 系統佈署指南 (部署至 Vercel)

當您準備將此系統推上線供團隊使用時：

### 步驟一：註冊並建立 Turso 雲端資料庫
1. 前往 [Turso 官網](https://turso.tech/) 免費註冊帳號。
2. 建立一個新的 Database，取得兩條重要資訊：
   - **Database URL** (通常長這樣：`libsql://您的資料庫名稱.turso.io`)
   - **Auth Token** (長長一串金鑰)

### 步驟二：推播至 GitHub
確定將本專案目錄推播至您的 GitHub 儲存庫。（若建立為 Public 儲存庫，請務必嚴格執行下方步驟！）

### 步驟三：於 Vercel 建立專案
1. 登入 Vercel，匯入您的 GitHub 專案。
2. 框架選擇保留預設的 `Next.js`。
3. **🚨 極度重要：設定環境變數 (Environment Variables)**：
   - `JWT_SECRET`：請在此打上一串只有你知道的亂碼密鑰（例如：`sk-djf3948jdjs9f3jdfs8hj...`）。
     > ⚠️ **資安警告**：如果你沒有在這裡設定 `JWT_SECRET`，系統會退回使用程式碼裡的預設字串。因為你的 Github 是公開的，這代表全世界都能用這個預設字串偽造最高權限進入你的後台！ 
   - `TURSO_DATABASE_URL`：填寫步驟一取得的 URL。
   - `TURSO_AUTH_TOKEN`：填寫步驟一取得的 Token。
4. 點擊 `Deploy`。部署完畢後，您將獲得一個公開的網址。

### 步驟四：上線後首要安全任務 (更改預設密碼)
> [!WARNING]
> 一旦第一位使用者連上雲端資料庫，系統會自動在雲端重建所有 Schema 並自動生成第一隻 `admin` 帳號。預設帳密為 `Maxupport` / `maxupport1238`。

因為這個預設帳號與密碼同樣寫在公開的原始碼中：
1. 請**立刻**存取你的 Vercel 網站網址。
2. 使用 `Maxupport` 與 `maxupport1238` 登入。
3. 導覽至「帳號設定」或首頁右上角，點選「修改密碼」，把這組大家都看得見的密碼換掉！
4. 完成以上動作後，您的系統就完全無懈可擊，可以放心讓團隊使用了。
